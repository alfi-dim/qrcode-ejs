<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Generated QR Codes</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 20px;
        }
        h1 {
            text-align: center;
            color: #333;
        }
        a {
            display: block;
            text-align: center;
            margin-bottom: 20px;
            color: #fff;
            background-color: #007BFF;
            padding: 10px 15px;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }
        a:hover {
            background-color: #0056b3;
        }
        ul {
            list-style-type: none;
            padding: 0;
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
        }
        li {
            background: white;
            border: 1px solid #ddd;
            border-radius: 5px;
            margin: 10px;
            padding: 15px;
            width: 250px;
            text-align: center;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            transition: transform 0.2s;
        }
        li:hover {
            transform: scale(1.02);
        }
        img {
            margin-top: 10px;
            border: 1px solid #ddd;
            border-radius: 5px;
        }
        p {
            margin: 5px 0;
            color: #555;
        }
    </style>
</head>
<body>
<h1>Generated QR Codes</h1>
<a href="/generate">Generate New QR Code</a>
<div>
    <% if (qrCodes.length > 0) { %>
        <ul>
            <% qrCodes.forEach(qrCode => { %>
                <li>
                    <p>Generated ID: <%= qrCode.codeId %></p>
                    <p>Generated By: <%= qrCode.generatedBy %></p>
                    <p>Generated At: <%= qrCode.generatedAt.toLocaleString() %></p>

                    <!-- Show the user's email if scannedByEmail is available -->
                    <p>Scanned By: <%= qrCode.scannedByEmail || 'Not Scanned Yet' %></p>
                    <p>Scanned At: <%= qrCode.scannedAt ? qrCode.scannedAt.toLocaleString() : 'Not Scanned Yet' %></p>
                    <img src="https://api.qrserver.com/v1/create-qr-code/?data=<%= qrCode.codeId %>&size=200x200" alt="QR Code" style="width: 200px;">
                </li>
            <% }); %>
        </ul>
    <% } else { %>
        <p>No QR codes generated yet.</p>
    <% } %>
</div>
</body>
</html>
