<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>QR Code Generated</title>
    <link rel="stylesheet" href="/styles.css">
    <style>
        body {
            margin: 0;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
        }
        div {
            text-align: center;
            background: #fff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
        }
        img {
            width: 500px; /* Set a smaller width for the QR code */
            height: auto;
            margin-bottom: 20px; /* Add space below the QR code */
        }
        #a {
            display: inline-block;
            margin-top: 20px;
            padding: 10px 20px;
            background-color: #007bff;
            color: #fff;
            text-decoration: none;
            border-radius: 5px;
        }
        #a:hover {
            background-color: #0056b3;
        }
        #generated {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 2;
        }
    </style>
</head>
<body>
<div>
    <h1>Your QR Code</h1>

    <p>Generated by: <%= qrCodeData.generatedBy %></p>
    <p>Generated at: <%= qrCodeData.generatedAt.toLocaleString() %></p>

    <!-- Display the QR Code ID -->
    <p>QR Code ID: <strong><%= qrCodeData.codeId %></strong></p>

    <!-- Display the generated QR code above the button -->
    <span id="generated">
        <img src="<%= qrCodeUrl %>" alt="Generated QR Code">
        <form action="/generate" method="POST">
         <button type="submit" id="a">Generate QR Code</button>
        </form>
    </span>
</div>
</body>
</html>
